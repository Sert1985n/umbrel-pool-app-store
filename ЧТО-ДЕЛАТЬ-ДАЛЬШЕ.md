# Что делать дальше

Полная пошаговая настройка — в **[SETUP-FULL.md](SETUP-FULL.md)**. Там всё одним списком: установка приложений, одна команда для coins.json и перезапуска, настройка пулов и кошельков на 8562, проверка дашборда 8561.

---

## Кратко

| Цель | Действие |
|------|----------|
| Настроить всё с нуля | Открыть **SETUP-FULL.md** и выполнить шаги 1–4. |
| Загрузить 28 монет на сервер | На Umbrel по SSH: `curl -sSL -o /tmp/install-coins-json.sh "https://raw.githubusercontent.com/Sert1985n/umbrel-pool-app-store/main/scripts/install-coins-json.sh" && sh /tmp/install-coins-json.sh` |
| Настроить пулы и кошельки | Открыть **http://ВАШ-IP:8562** → Pool Configuration → Refresh Master Coin List → Setup Database Schema → Generate Pool Config File → заменить "xxx" на адреса → перезапустить MiningCore. |
| Увидеть таблицу пулов | После настройки открыть **http://ВАШ-IP:8561** (Pool Dashboard). |
| Записать изменения в GitHub | `git add . && git commit -m "..." && git push origin main` |
