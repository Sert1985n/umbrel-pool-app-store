version: "3.7"

services:

  app_proxy:
    environment:
      APP_HOST: sert-umbrel-pool-miningcore-webui_web_1
      APP_PORT: 80
  web:
    image: nginx:alpine
    restart: on-failure
    volumes:
      - ./www:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
