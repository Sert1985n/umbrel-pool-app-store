version: "3.7"

services:

  app_proxy:
    depends_on:
      - server
    environment:
      APP_HOST: sert-umbrel-pool-dashboard_server_1
      APP_PORT: 80

  server:
    image: ghcr.io/sert1985n/pool-dashboard:latest
    restart: on-failure
