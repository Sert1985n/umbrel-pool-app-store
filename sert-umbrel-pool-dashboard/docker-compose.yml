version: "3.7"

services:

  app_proxy:
    depends_on:
      - web
    environment:
      APP_HOST: ${APP_ID}_web_1
      APP_PORT: 80
  web:
    image: nginx:alpine
    restart: on-failure
    volumes:
      - ./www:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
