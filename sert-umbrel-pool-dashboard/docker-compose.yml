version: "3.7"

services:

  app_proxy:
    depends_on:
      - server
    environment:
      APP_HOST: sert-umbrel-pool-dashboard_server_1
      APP_PORT: 80

  server:
    build: .
    image: sert-umbrel-pool-dashboard-web
    restart: on-failure
