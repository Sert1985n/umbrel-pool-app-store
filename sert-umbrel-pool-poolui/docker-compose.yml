version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: sert-umbrel-pool-poolui_server_1
      APP_PORT: 80
    depends_on:
      - server

  server:
    build: .
    image: sert-umbrel-pool-poolui:latest
    restart: on-failure
